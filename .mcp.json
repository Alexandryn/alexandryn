{
  "mcpServers": {
    "postgres": {
      "type": "stdio",
      "command": "sh",
      "args": [
        "-c",
        "set -a; . \"$(git rev-parse --show-toplevel)/.env\"; set +a; exec npx -y @modelcontextprotocol/server-postgres \"$DATABASE_URL\""
      ],
      "env": {}
    }
  }
}