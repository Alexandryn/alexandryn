/**
 * Shared focus-visible ring, using the design reference's own
 * :focus-visible{outline:2px solid var(--ac)} rule (accent token) —
 * one place to change if the ring's color/width/offset ever needs to.
 */
export const FOCUS_RING =
  'focus-visible:outline focus-visible:outline-2 focus-visible:outline-offset-2 focus-visible:outline-accent'
