{
  "name": "alexandryn",
  "private": true,
  "description": "Workspace root (ADR 0008). The Go module lives at the repo root; the two JavaScript packages are workspaces.",
  "workspaces": [
    "web"
  ],
  "engines": {
    "node": ">=24"
  }
}
